#include "HAL/hal.h"
#include "epd_views/views.h"

void setup() {
  hal_init();
  // screenClear();
  // delay(2000);
  // create_view_bw(txt_reader, 0);
  // delay(2000);
}

/* The main loop
 * -------------------------------------------------------------*/
void loop() {}
